version: "3.8"

services:
  backend:
    container_name: movie-api

    build: .

    ports:
      - "3000:3000"

    volumes:
      - .:/app
      - /app/node_modules

    command: npm run dev

    restart: unless-stopped